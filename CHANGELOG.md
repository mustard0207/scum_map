# 更新日志

## v1.5.0 (2026-07-09 03:00:00)

### 新增功能
- **上线全新“园艺种植助手”模块**：
  - 基于最新游戏数据整理了 28 种农作物的硬核参数（生长温度、发芽温度、耐寒、病虫害预警及产物），助你在废土成为金牌农夫。
  - **物理级交互阅读**：引入底层 WXS 手势引擎，将长篇枯燥的园艺种植百科，做成了可丝滑拖拽展开/折叠的底部交互面板，体验媲美原生系统 UI。

### 优化
- **首页 UI 彻底重构（极简方案）**：
  - 去除首页冗余的硕大主图和重复标题，采用极具高级感的**极简顶部导航栏布局**。
  - 引入全新的精简版正方形 Logo，并利用 CSS 技巧使其无缝嵌入左上角状态栏，释放了大量的纵向视野空间，完美适配各种奇葩比例屏幕。
- **园艺数据视觉打磨**：
  - 弃用容易在 iPad 等宽屏设备上因 `rpx` 等比缩放导致“巨无霸”特效的布局，全系换用物理级 `px` 与智能 Flex 双排布阵。
  - 严格规范了数据卡的字体层级关系与加粗规范，做到毫厘必争的视觉对齐。
- **武器库文案对齐**：为带有特定扩展包属性的武器（如猎手弓等）补充了“(DLC)”后缀标记。
## v1.4.3 (2026-07-07 13:00:00)

### 修复
- **地图自定义标记导入修复**：修复了在导入包含载具(Vehicle)的自定义标记代码时报 `emoji is not defined` 的致命错误；修复了“导入成功”的 Toast 提示语被截断的问题；优化了清空标记功能的二次弹窗排版，将“确定”与“取消”按钮修改为更符合直觉的左确右取。
- **PC端窗口拉伸越界 Bug 修复**：深度重构了地图 `tile-map` 底层 `gestures.wxs` 的网格线渲染算法。废弃了原本依赖 `bottom / right` 以及受容器尺寸滞后影响的定位方式，转为使用硬计算绝对值的 `width / height`。同时为地图页补充了 `onResize` 生命周期监听，彻底解决了在 PC 微信端拖拽缩放窗口时，导致游戏网格分隔线疯狂变长溢出屏幕的问题。
- **全屏引导遮罩自适应修复**：统一了全局各功能模块（地图、狩猎助手、地堡计算器、保险丝计时器）首屏引导遮罩的底层 HTML 结构，并重构了遮罩的 CSS Flex 弹性盒定位方案（剥离死板的 `justify-content: center`，引入原生滚动与 `margin: auto` 的高阶自适应）。彻底解决了在 iPad 等大尺寸方屏设备、或横屏状态下，遮罩内容因高度超出屏幕而向上下两端溢出导致无法滚动、无法点击到底部按钮的致命排版 Bug。

## v1.4.2 (2026-07-06 19:30:00)

### 修复
- **地堡工具UI适配**：优化废弃地堡计算器在小屏幕机型上的垂直空间利用，修复了底部键盘和按钮被遮挡的 Bug，并引入底部安全区（`safe-area-inset-bottom`）托起。
- **地图底栏对齐校准**：修复了地图页面底部的操作栏中，由于原生按钮与文本节点基线不一导致的图标与文字轻微错位问题，同时补充了由于 CSS 权重冲突丢失的底部安全区避让。

## v1.4.1 (2026-07-06 18:00:00)

### 优化 (狩猎助手体验重构)
- **底层架构切换 (WXS 原生物理引擎)**：将狩猎助手的底部数据面板从传统的固定高度与卡顿的 JS 动画，彻底重构为类似 iOS Bottom Sheet 的可拖拽物理抽屉。利用 WXS 直接在渲染层执行 60 帧无损动画运算与手势路由拦截，实现带有橡皮筋阻力的极致流畅回弹手感。
- **全屏高度自适应**：彻底抛弃死板的 `60vh` 高度，抽屉展开时智能覆盖最高 85% 的屏幕，折叠时自适应剩余空间。在任何比例的手机上，均能完美保持主地图的“完全正方形”视野比例，边缘对齐零死角。
- **动态手势与滚动解耦**：彻底解决了面板拖拽与内部大段文本滚动 `<scroll-view>` 之间的互斥冲突。折叠状态拖动文字即触发面板拉起，展开状态自动激活文字滑动阅读，体验宛如原生级 APP。
- **战术坐标系 UI 同步**：将狩猎助手全局操作栏的坐标信息展示 UI 与主地图的“废土战术风格”全面对齐。统一采用了半透明暗金黑底色 (`rgba(28,26,16,0.9)`)、警示黄字体 (`#FFB000`) 与等宽字体序列，根除了各功能模块间的设计割裂感。
- **沉浸式暗场遮罩 (Overlay)**：拉起抽屉时动态渲染地图底层黑场遮罩，并自动隐去地图边缘的复位按钮与缩放拖拉条，防止误触且强化阅读沉浸感。
- **引导手册更新**：全面更新狩猎助手的首次进入引导文案，新增针对探查模式的“同类型高亮”说明与图鉴模式的情报展示提醒，降低新手破冰难度。

### 修复
- **动画瘫痪 Bug (NaN Error)**：深度修复了由于微信小程序框架在页面挂载首帧下发空属性对象，导致的 WXS 引擎坐标计算变量被污染为 `NaN` 的致命 Bug。增加了强类型判定与生命周期锁，确保抽屉不论何时都能顺利渲染不崩溃。
- **体验阻断修复**：修正了此前的“全自动弹起”交互逻辑漏洞。现在在点选动物图鉴或使用十字准星探测时，面板不会再强制弹起遮挡地图，而是默默刷新数据，完美迎合了用户“边选边对比地图圈圈”的核心操作流。

## v1.4.0 (2026-07-06 16:04:21)

### 新增功能
- **上线全新“狩猎助手”模块**：
  - 基于游戏底层 8.7 万字的地形文件深度解析，构建了完全像素级还原的大陆级动植物分布图。
  - **动物图鉴模式**：支持一键选中查看特定动物的“栖息地坐标分布”与“喜好诱饵”情报，并在全局地图动态高亮圈出猎物活动范围。
  - **探索探测模式**：通过准星扫描地图上任意地点，反查该区域的环境植被类型及潜在出没野生动物，辅以丝滑的雷达波纹与震动反馈，极大提升代入感。

### 优化
- **全局 UI/UX 深度统一化与组件级重构**：
  - 移除了微信默认导航栏，对所有底层业务工具（狩猎助手、武器数据手册等）强制接入 SCUM 废土风格的 `nav-bar` 与沉浸式全屏布局。
  - **核心控件“滤油”改造**：剔除了原生微信极丑的 `<switch>` 滑块，纯手写了一套极客复古 CSS 风格的 `.filter-toggle` 开关（琥珀色与暗金背景），并对齐武器数据手册的中英切换与地图图层切换。
  - 全局补齐带有“毛玻璃遮罩”的高级引导弹窗，统一用户在新工具里的破冰体验。
  - 彻底打通双语模块（图鉴与探测页面的动态双语环境加载），并对“城市废墟”地形的图层覆盖渲染颜色进行高对比度色值（`#D81B60`）校正修复，彻底解决在深色地貌下看不见的 Bug。

## v1.3.0 (2026-07-06 15:45:00)

### 新增功能
- **上线武器数据手册**：在主页下方增设入口，收录了游戏内上百款枪械的各项硬核参数（伤害、射速、适配弹药及附件），采用防爆内存的懒加载手风琴式折叠列表呈现。
- **双语极速引擎**：武器名称与口径全面接入中英对照双数据集，通过独立开关无缝切换；重构底层逻辑，彻底脱离网络请求，实现数据100%本地化极速秒开渲染。
- **智能检索与独立筛选体验升级**：
  - 新增原生的“弹药口径”底部滚轮过滤系统，通过多维属性字典配合（可同时锁定“特定武器分类” + “特定口径”），带来极客级的查阅快感。
  - 将原生 `[口径]` `[射速]` `[射程]` `[伤害]` 数据并列为四宫格点击排序条，解耦并完善了跨字段多维组合排序算法（可完美识别 `700 RPM` 等含单位文字串并提取数字）。
- **内容安全防封级重构**：
  - 全工程剥离危险命名汇，入口与分享文案由“武器图鉴”更名为安全属性满分的“武器数据手册”。
  - **重要坑点修复**：针对微信小程序极度严苛的文本输入内容机器审查机制，通过引入原生组件与交互改版，**彻底移除了具备被封杀隐患的自定义文本搜索框**。

## v1.2.2 (2026-07-06 12:30:00)

### 优化
- **地图分享逻辑深度优化**：
  - 动态分享隐私保护：分享当前地图页时，会自动检测左上角“自定义标记”的总开关状态。如果处于隐藏状态，分享的链接或卡片中将自动过滤掉所有私人标记数据，严格保护用户隐私。
  - 单点靶向分享文案：当只选中 1 类地图固定探索点（如加油站）进行分享时，微信聊天列表的分享卡片标题会自动将其转化为精准的中文名（例如“我给你分享了钓鱼点”），替代模糊的“1类探索点”。
  - 补齐合规化组件：按照微信原生开发防封规范，强制引入了缺失的 `onShareTimeline` 接口，现在支持无缝将带有当前标记点数据与视角的地图转推至朋友圈。

## v1.2.1 (2026-07-05)

### 优化
- **UI 风格全面对齐**：彻底修复了地堡工具栏内部二级页面的 CSS 样式断层问题，剔除多余的纯白色，严格采用全局设定的 `rpx` 弹性布局与“琥珀 CRT 终端”主题色（暗金底色，发光边框，`:active` 点击高亮）。
- **交互震动重构与自查修复**：补齐了地堡计算器防爆数字小键盘中 10 余个按键的马达点击短震（`wx.vibrateShort`），并且修复了计算成功时 1 毫秒内由于双重调用可能引发的机型震动冲突。另外在保险丝断电状态下，点击按钮虽被逻辑拦截，但依然保留震动反馈以维持手感。
- **首次引导遮罩（全局组件化）**：将大地图中的“毛玻璃暗金引导卡片”抽象至 `app.wxss` 并在全应用级复用。为废弃地堡计算器与保险丝计时器分别添加了纯净版（去油版）的首发引导，配合 `wx.getStorageSync` 缓存实现“一次提醒，终身免疫”。
- **智能输入引擎与深度防呆（地堡计算器）**：
  - 顶部基础数据框（初始值/目标值）实施强类型锁定，全面屏蔽符号输入。
  - 重构 8 行操作区输入逻辑，加入“数字结尾智能跳格”系统，允许用户连续敲击 `+5-2*10` 自动完成跨格录入。
  - 加入空符号替换机制（例如输入 `+` 后再按 `-` 可直接替换，不会产生 `+-` 乱码）。
  - 全局拦截断层输入：当操作框内仅有符号而未输入数字时，不仅会拦截“换格”与“开始破解”操作，还会触发长震动警告，彻底杜绝逻辑跑飞。
- **合规化与去敏感词（地堡计算器）**：移除了计算器中所有的“破解”字眼，全部平滑替换为“推算”、“计算”，防患于未然，彻底规避小程序因疑似“外挂辅助”引发的机审封禁风险。
- **UI 细节优化与全局排版扩充**：
  - 对废弃地堡计算器的红蓝双通道目标框增加了专属的透明边框与 Focus 状态同色发光投影（Red/Blue），极大提升了视觉辨识度与操作一致性。
  - 全面提升使用说明遮罩及全站功能菜单子标题的默认字号（提升至 `26rpx`），并拉宽了横向屏显利用率，进一步释放了原本局促的纵向空间。文案上完成了全面精简重排。

## v1.2.0 (2026-07-05)

### 新增
- **保险丝计时器**：新增专为 SCUM 废弃地堡设计的“保险丝计时器”辅助工具，支持 200A~1000A 的多档位安培数选择与 1%~100% 精确耐久度快速步进录入。
- **脱机防断电倒计时引擎**：底层采用“时间戳比对法”重构定时器逻辑，并调用系统屏幕常亮接口（`wx.setKeepScreenOn`），彻底杜绝微信切入后台或手机自动息屏导致的倒计时冻结、不准等硬核致命伤。
- **零阻力盲按 UI 系统**：深度定制“废土机能风”防暴走操作面板，彻底移除原生键盘弹出，采用特大号步进器 `[-10][-1][+1][+10]` 和全宽底栏开关；每个按钮点击附带干脆利落的马达物理震动反馈（`wx.vibrateShort`），满足高压盲按需求。运行态时自动锁定上部参数区防误触。
- **高保真 AI 女声播报**：引入系统 AI 女声语音流。在倒计时的 10分钟、5分钟、2分钟、1分钟、30秒及归零时，触发清晰无损的人声预警（通过本地离线载入 `assets/audio/` 中的未压缩 `.wav` ），配合后期屏幕呼吸血红闪烁，拉满逃生压迫感。

## v1.1.0 (2026-07-05)

### 新增
- **废弃地堡计算器**：新增辅助工具，专为 SCUM 游戏中的废弃地堡读卡器设计，支持一键暴破穷举 8 开关的解密路线。
- **自定义数字键盘**：为地堡计算器单独开发了沉浸式的防爆小键盘，按照人体工学和九宫格逻辑定制（0居中对齐，支持快速删除和换格），提供纯正终端操作体验。
- **全局色彩系统升维**：所有二级菜单和新工具的界面色彩已无缝对接小程序的主题色（生存终端琥珀亮金 `#FFB000`）。
- **工具列表大屏化**：重新设计了首页地堡工具入口，Emoji 符号（🗺️、🧮等）与卡片布局整体放大 30%，视觉更张扬且不易误触。

### 修复
- **单页分享死锁漏洞**：修复了小程序开发中的经典缺陷。如果你把大地图或某个工具直接分享给好友，由于历史栈帧为空，好友在空降页面点击左上角的“返回”箭头时会卡死。现在已加入栈帧智能探针，遇到空降场景时点击返回将触发自动逃逸机制（`wx.reLaunch`），瞬间传送到大地图首页！
- **样式级联污染**：剔除了 `app.wxss` 中全局 `.btn-primary` 的过厚 `padding` 对地堡工具内部按钮的体积挤压，确保所有操作面板绝对一比一对称。
## v1.0.2 (2026-07-04)

### 新增
- **载具细分与完整底盘管理**：全面扩容细分车型，支持包括拉各（皮卡）、太众、莱卡、拖拉机、三轮/巡航/越野摩托及单车等类型。
- **动态匹配图标**：地图上的车辆标记将精确渲染不同车型的独立图标（如 🚜、🚚），不再使用单一通用图标。
- **残缺载具提示**：为残缺（缺少配件）的车辆增加废土暗橙色 (`#C85A17`) 背景和内阴影强调，实现静态区分；且过期红色闪烁警报依然保持最高优先级。
- **按需折叠交互**：完好的车辆默认折叠繁杂的底盘网格，提供极简的“随手标记”体验；点击“标记残缺”或已缺失配件时自动平滑展开。

### 优化
- **无感数据兼容升级**：在极简的 16 位底层序列化协议下，通过配件位掩码重叠复用机制（主驾/前座共享，副驾/后座共享），在完全不破坏旧版分享链接和历史本地备份的基础上，完美扩充了 5 个车身/座椅新配件部位。
- **UGC 合规与防封禁**：移除了信息窗口中允许玩家自由输入文本的自定义输入框。彻底规避了因内容安全识别 (msgSecCheck) 未接入而引发的微信下架封禁风险，改为动态显示安全的纯净系统名（例如 `“载具 - 莱卡”`、`“储物箱”`、`“未定义标记”`）。
- **底盘网格布局重构**：重构了底盘的三列 flex 排版系统，完美支持将发电机、电瓶、座椅等配件严格按照游戏真实物理布局对齐左右两侧。
- **自定义标记置顶优化**：为自定义标记和系统固定点（POI）分配了独立的 Z轴高度 (`z-index`)，确保玩家自己添加的所有标记永远置顶显示在 POI 之上，彻底解决密集重叠区域的遮挡问题。

## v1.0.1 (2026-06-29)

### 新增
- **首次使用引导**：进入地图页自动弹出引导遮罩，介绍 5 个核心功能
  - 中心光标定位、坐标粘贴、兴趣点筛选、导入导出备份、标记过期机制
  - 点击"我知道了"后不再显示（`wx.setStorageSync` 持久化）
  - 半透明毛玻璃背景，可看到背后地图
- **底部按钮副标签**：5 个底栏按钮各增加一行灰色说明文字
  - 输入→粘贴游戏坐标、选点→标记光标位置、分享→分享当前标记、管理→导入导出标记、筛选→显示兴趣地点
- **坐标输入弹窗增强**：补充 Ctrl+C 获取坐标说明和支持的格式示例
- **筛选面板说明**：顶部新增提示条解释"自定义标记"与"兴趣地点"的区别，footer 补充 5 个限制仅针对兴趣地点
- **管理抽屉说明**：顶部新增 SCUM# 代码用途说明
- **导入弹窗增强**：补充格式说明和"追加不覆盖"行为提示
- **信息窗口提示**：自定义标记操作区新增类型选择和过期机制说明

## v1.0.0 (2026-06-29)

### 新增
- **自定义标记筛选**：地点筛选面板新增"自定义标记"大类
  - 支持按类型筛选：📌 无类型 / 🏠 房屋 / 🚗 载具 / 📦 储物箱
  - 大类总开关：一键开启/关闭所有自定义标记
  - 小类独立开关：可按类型显示/隐藏，各类型显示对应标记数量
  - 大类开关联动：所有小类关闭时大类自动关闭，开启大类时小类全开
  - 筛选不影响数据：被隐藏的标记保留在存储中，取消筛选后恢复显示
- **标记有效期系统**：自定义标记支持维护时效管理
  - 每个标记记录放置时间（精确到秒），点击放置时间可修改日期
  - 信息窗口显示剩余时间（X天X小时X分）
  - 快过期的标记在地图上红白闪烁边框提醒（默认剩余 <2 天），所有标记闪烁同步
  - "刷新有效期"按钮一键重置放置时间
  - 可配置各类型默认过期天数（房屋 15 天、载具 10 天、箱子 15 天、无类型永不过期）
  - 可配置最小提醒天数阈值
  - 分享链接携带过期配置，接收方可复用分享者的设置
- **清空所有标记**：管理菜单新增"清空所有标记"功能，带二次确认弹窗
- **标记上限提升**：自定义标记上限从 50 提升到 100，与 POI 标记独立计数

### 修复
- **分享遗漏标记**：修复底栏分享时因 `selectedMarker` 残留导致只分享单个标记的 bug（增加 `showInfoWindow` 检查）
- **弹窗误关闭**：修复信息窗口、导入弹窗、备份抽屉、保存确认弹窗点击内容区域意外关闭的 bug（`catchtap=""` → `catchtap="noop"`）
- **分享提示条遮挡**：查看分享内容时，坐标面板、全局视角按钮、缩放拖拉条自动上移避免被提示条覆盖

### 优化
- **术语优化**：筛选面板"POI 筛选"改为"地点筛选"，分享卡片"分类POI"改为"探索点"
- **菜单重命名**：底部栏"备份"改为"管理"，"导入标记"改为"导入自定义标记"，"导出标记"改为"导出自定义标记"
- **首页描述**：改为"查看和标记游戏地图位置"
- **版本号重置**：正式发布版本 v1.0.0
- **技术文档**：新增 `catchtap=""` 避坑备忘案例（8.4 节）

---

## v8.0 (2026-06-28)

### 优化
- **POI 筛选面板交互优化**：
  - 移除顶部全局"已选 X/5"计数显示
  - 每个大类行显示该大类内小类的选中情况（如"2/5"），帮助用户快速定位需要调整的大类
  - 展开大类时提前加载小类点位数量，避免延迟显示

---

## v7.9 (2026-06-26)

### 新增
- **自定义标记类型**：用户标记支持 3 种类型分类
  - 🏠 房屋 / 🚗 载具 / 📦 储物箱
  - 类型带绿色圆形背景色，地图上一眼可辨
  - 信息窗口内类型选择器，点击切换/取消
  - 副标题显示类型名（无类型时显示"标记 #N"）
- **本地数据持久化**：用户标记和 POI 筛选状态自动保存到本地存储
  - 正常打开小程序自动恢复上次的标记和筛选
  - 用户添加/删除/修改标记、切换筛选时自动保存
  - 手动清除筛选或重置视图时同步更新存储
- **分享链接临时查看**：打开分享链接不覆盖本地存档
  - 底部显示"正在查看分享内容"提示条 + 保存按钮
  - 点击保存弹出确认弹窗（三按钮：覆盖前备份 / 取消 / 确定）
  - "覆盖前备份"导出本地旧存档到剪贴板
  - 用户在分享页自行操作标记/筛选时自动接管为本地数据
- **POI 标记分享**：POI 信息窗口新增分享按钮（无删除按钮），可单独分享任意 POI 点位
- **分享增强**：
  - 信息窗分享按钮 → 分享单个标记（含类型信息）
  - 底部栏分享按钮 → 分享自定义标记 + POI 筛选分类 ID
  - 接收方自动恢复 POI 筛选状态并加载对应分类点位
- **坐标输入提示**：优化提示文案为"将游戏内地图的坐标粘贴到此处，即可在离线地图上标记该位置，方便路线规划与点位记录"

### 合规整改
- **首页"即将推出"占位按钮**：删除未完工的"更多工具"卡片，避免测试版驳回
- **分享标题静态化**：单标记分享标题改为固定文案"我在SCUM地图上标记了一个位置"，阻断 UGC 敏感词传播
- **坐标提示文案去敏感词**：去掉"Ctrl+C"、"游戏内地图按"等可能被误判为外挂辅助的措辞
- **免责声明**：首页底部增加"玩家自制工具，非官方出品。数据自己整理，仅供参考。"
- **首页 logo**：替换为实际 logo 图片（200×109，23KB），包体内置秒加载
- **统一命名**：所有界面"SCUM 地图"统一为"SCUM游戏地图"
- **用户隐私保护指引**：已在微信公众平台提交剪贴板隐私声明
- **网络域名配置**：瓦片 CDN 域名已配入微信后台合法域名

### 修复
- **标记编号错乱**：修复自定义标记编号被 POI 标记干扰的问题，编号仅计算用户标记
- **导出含 POI**：导出和分享只包含用户自定义标记，不再混入 POI 标记
- **分享 selectedMarker 未声明**：修复 `onShareAppMessage` 中 `selectedMarker` 变量未声明的 bug
- **关闭信息窗 selectedMarker 残留**：`closeInfoWindow` 现在清除 `selectedMarker`，防止底部栏分享误走单标记路径
- **二进制格式向后兼容**：编码版本字节从 `0x53` 升级为 `0x54`（含类型字段），解码器按版本区分，旧数据安全导入

### 优化
- **默认标记名称**："未命名标记"改为"自定义标记"
- **导出/分享编码**：标记类型字段随导出和分享链路完整保留
- **导出提示**：Toast 文案改为"代码已复制到剪贴板，可以粘贴到其他地方保存备用"，显示时长加倍

---

## v7.8 (2026-06-26)

### 新增
- **POI 分类筛选功能**：支持按分类显示/隐藏系统 POI 标记点
  - 精选模式：地堡、载具、辐射 3 个可展开大类 + 警局、药店、加油站 3 个直选小类
  - 完整模式：12 个大类全部可展开，102 个小类可自由筛选
  - 最多同时开启 5 个小类
  - 默认显示前哨站，可手动关闭或清除
- **POI 数据来源升级**：从 scum-map.com GraphQL API 获取完整分类数据
  - `category-map.js`：102 个分类定义（section/emoji/cnName）
  - `分类/*.js`：102 个点位文件，7614 个 POI 标记点
  - 按 section 合并为 12 个数据文件，按需加载
- **标记 emoji 化**：所有 POI 标记使用 emoji 图标，不同分类用不同 emoji 区分
- **标记渲染优化**：用户标记使用 📌（图钉），POI 标记使用各自 emoji
- **渲染限流**：全局视角每区每小类最多 10 个点，放大 ≥6x 显示全部，硬上限 1000
- **清除筛选按钮**：一键清除所有已选分类
- **数据更新脚本**：`tools/merge-poi-data.js` 将 102 个分类文件合并为 12 个 section 文件

### 修复
- **筛选面板误关闭**：重构事件结构，遮罩层与面板分离，消除嵌套 `wx:for` 下 `catchtap` 不可靠的问题
- **筛选开关无视觉反馈**：改用 `activePoiMap` 对象查找表替代 `indexOf`，解决 WXML 模板中数组方法不可靠的问题

---

## v7.7 (2026-06-26)

### 修复
- **坐标输入弹窗误关闭**：修复点击输入框或对话框区域时弹窗意外关闭的问题。根因是 `catchtap=""`（空字符串）在微信小程序中无法可靠拦截事件冒泡，改为 `catchtap="noop"`（真实方法引用）确保 tap 事件不冒泡到 mask 层。

### 优化 (WXS 高清与高性能重构)
- **解决地图模糊问题**：彻底移除瓦片地图容器上的 `transform: scale` 缩放属性，改为在 WXS 中直接通过底层渲染节点更新物理尺寸，确保地图缩放时的高清渲染（零锯齿/原画质）。
- **解决滑动卡顿问题**：引入 WXS 渲染接管机制（`gestures.wxs`），将所有手势拖拽、惯性计算、捏合缩放从 JS 业务层完全迁移至视图层（渲染线程）独立运算，告别因高频 `setData` 通信造成的卡顿，实现满帧极致顺滑。
- **动态坐标补偿机制**：重构 `onMarkerTap` 的屏幕像素反推算法，在零通信成本的基础上兼顾了业务层标记窗口原本对点击坐标 `screenX/Y` 的依赖。
- **渲染中断保护**：在 WXS 与 JS 通信机制中增设了 `abort` 强制中断信号，完美防止因惯性滑动与 JS API 坐标跳转（如“回到中心”、“定位至标记”）同时发生导致的画面抖动和坐标穿透回弹。

---

## v7.6 (2026-06-26 01:49:06)

### 新增
- **Z3+Z4+Z6 瓦片分层加载**：根据缩放级别动态切换瓦片层级
  - Z2 (scale < 1.0)：本地 JPG 底图
  - Z3 (1.0 ~ 1.5)：jsDelivr 网络 webp，2×2 网格
  - Z4 (1.5 ~ 3.0)：jsDelivr 网络 webp，4×4 网格
  - Z6 (scale ≥ 3.0)：jsDelivr 网络 webp，16×16 网格，Z4 作为兜底
- **缩放拖拉条**：右侧垂直拖拉条，以十字准星为中心缩放，支持鼠标和手指操作
- **本地缓存机制**：网络瓦片首次加载后自动缓存到用户本地，后续直接读取

### 优化
- 修复 `_clampOffset` 时序 bug（scale 先更新再钳制边界）
- 坐标面板与全局视角按钮位置互换
- 坐标标签从"经度/纬度"改为"X/Y"，与游戏坐标格式一致
- 地图区域使用动态 top/bottom 定位，消除底栏黑边
- 移除关于页面和 scum-map.com 数据来源引用
- 添加 debug 日志和 `wx.getWindowInfo` 防御

---

## v7.5 (2026-06-25 22:21:40)

### 新增
- **Z4 网络瓦片加载**：jsDelivr CDN + 本地缓存机制
- Z4 瓦片缓存：`wx.downloadFile` + `fs.saveFile` 持久化存储

### 优化
- 移除本地 Z4 瓦片（16 张 JPG），减小包体积
- 瓦片计算合并到 `_refreshOverlayAnim` 的 setData 中，不增加桥接调用次数

---

## v7.4 (2026-06-25 22:21:40)

### 新增
- **Z4 瓦片分层加载**（本地版本，后改为网络加载）
- 瓦片可见性计算和变化检测机制

---

## v7.3 (2026-06-25 19:59:21)

### 优化
- 性能优化检查记录
- 风险表更新

---

## v7.2 (2026-06-25 14:50:47)

### 新增
- 信息窗口重构（标题区 + 坐标区 + 操作区）
- 底部操作栏重构
- 浮动控件（回到全局视角按钮）

---

## v7.1 (2026-06-25 14:10:41)

### 新增
- 标记导入导出（二进制紧凑格式，4 位小数精度）
- 底部栏优化

---

## v7.0 (2026-06-25 14:10:41)

### 新增
- 自定义手势地图引擎（拖拽、惯性、双指缩放、双击缩放）
- 坐标系统（屏幕坐标 ↔ 地图像素坐标 ↔ 游戏坐标）
- 坐标跳转（支持游戏原始格式和简单格式）
- 多点标记（上限 50 个）+ 信息窗口
- 区域网格系统（5×5 网格，D4→Z0）
- 免费社交分享（微信群聊卡片）
- 零延迟地图（JPG 底图本地内置）
